import db from '../../db/connection'

/**
 * Insert information of user into user table
 * @param {String} nickname
 * @param {String} email
 * @param {String} password
 * @param {Function} cb
 */
export const createUser = (nickname, email, password, cb) => {}
